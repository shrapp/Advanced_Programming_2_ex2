@model IEnumerable<MessagesApp.Models.Contact>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nickname)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Server)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nickname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Server)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.User">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.User">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.User">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
